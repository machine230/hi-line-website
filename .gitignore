.DS_Store
*.broken
node_modules/

# Local environment secrets — never commit these
.env
.env.local
.env*.local

# Local editor/tooling config and Supabase CLI scaffold — keep out of the repo
.claude/
supabase/
